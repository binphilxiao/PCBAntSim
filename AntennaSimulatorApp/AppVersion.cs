namespace AntennaSimulatorApp
{
    /// <summary>
    /// Single source of truth for the application version.
    /// Update <see cref="Current"/> when releasing a new build.
    /// </summary>
    public static class AppVersion
    {
        public const string Current = "1.0.2";
    }
}
