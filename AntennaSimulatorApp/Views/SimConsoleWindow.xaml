<Window x:Class="AntennaSimulatorApp.Views.SimConsoleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="openEMS Simulation"
        Width="800"
        Height="520"
        MinWidth="600"
        MinHeight="400"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="True"
        Closing="Window_Closing">

        <Grid Margin="12">
                <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Status bar -->
                <DockPanel Grid.Row="0"
                           Margin="0,0,0,8">
                        <TextBlock x:Name="TxtStatus"
                                   Text="Ready"
                                   FontWeight="SemiBold"
                                   FontSize="13"
                                   VerticalAlignment="Center"/>
                        <TextBlock x:Name="TxtElapsed"
                                   Text=""
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Center"
                                   Foreground="Gray"
                                   FontFamily="Consolas"/>
                </DockPanel>

                <!-- Progress bar -->
                <DockPanel Grid.Row="1"
                           Margin="0,0,0,8">
                        <TextBlock x:Name="TxtProgress"
                                   DockPanel.Dock="Right"
                                   Text=""
                                   Foreground="Gray"
                                   FontFamily="Consolas"
                                   FontSize="11"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0"
                                   MinWidth="120"
                                   TextAlignment="Right"/>
                        <ProgressBar x:Name="ProgressSim"
                                     Height="14"
                                     Minimum="0"
                                     Maximum="100"
                                     Value="0"
                                     Visibility="Collapsed"/>
                </DockPanel>

                <!-- Console output -->
                <Border Grid.Row="2"
                        BorderBrush="#555"
                        BorderThickness="1"
                        CornerRadius="3"
                        Background="#1E1E1E">
                        <TextBox x:Name="TxtConsole"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Disabled"
                                 Background="#1E1E1E"
                                 Foreground="#DCDCDC"
                                 FontFamily="Consolas"
                                 FontSize="12"
                                 BorderThickness="0"
                                 Padding="8"
                                 AcceptsReturn="True"/>
                </Border>

                <!-- Buttons -->
                <StackPanel Grid.Row="3"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,10,0,0">
                        <Button x:Name="BtnViewResults"
                                Content="📈 View Results"
                                Width="120"
                                Height="30"
                                Margin="0,0,8,0"
                                Click="BtnViewResults_Click"
                                ToolTip="Run post-processing and show S11 results now"
                                IsEnabled="False"/>
                        <Button x:Name="BtnOpenFolder"
                                Content="Open Folder"
                                Width="100"
                                Height="30"
                                Margin="0,0,8,0"
                                Click="BtnOpenFolder_Click"
                                IsEnabled="False"/>
                        <Button x:Name="BtnStop"
                                Content="Stop"
                                Width="90"
                                Height="30"
                                Margin="0,0,8,0"
                                Click="BtnStop_Click"
                                IsEnabled="False"/>
                        <Button x:Name="BtnClose"
                                Content="Close"
                                Width="90"
                                Height="30"
                                Click="BtnClose_Click"/>
                </StackPanel>
        </Grid>
</Window>
